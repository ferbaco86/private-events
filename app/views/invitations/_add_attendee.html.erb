<%= form_with model: @invitation do |invitation_form| %>
  <%= invitation_form.collection_select(:event_attendee_id, User.all, :id, :name) %>
  <%= invitation_form.collection_select(:attended_event_id, Event.all, :id, :description) %>
<% end %>