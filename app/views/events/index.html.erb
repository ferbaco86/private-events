<div class="card">
  <div class="card-content">
    <h3 class="title is-5">Events</h3>
  </div>
</div>
<div class = "card">
  <div class = "card-content">
    <ul>
      <% @records.each do |record| %>
        <li>
          <strong>Event_id: </strong><%= record[0] %>
          <strong>Creator Name: </strong><%= record[1].first.name.capitalize %>
        </li>
      <% end %>
    </ul>
  </div>
</div>

